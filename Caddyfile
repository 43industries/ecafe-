{
    admin off
    persist_config off
    auto_https off

    frankenphp
}

:{$PORT:8000} {
    root * /app/public
    encode zstd gzip
    php_server
    try_files {path} {path}/ /index.php?{query}
}
